# Self-Synthesizing Autonomous DBMS
This is an experimental AI-driven database that builds its own schema and optimizes performance using C++ JIT.

## How to run:
1. Install requirements: `pip install google-generativeai streamlit pandas`
2. Run Engine first: `python main_engine.py`
3. Launch Dashboard: `streamlit run app.py`